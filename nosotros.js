/**
 * nosotros.js — nosotros.html
 * This page has no interactive behavior of its own beyond the shared
 * nav/footer/WhatsApp chrome.
 */

import { initSiteChrome } from '../core/site-chrome.js';

document.addEventListener('DOMContentLoaded', () => {
  initSiteChrome('nosotros.html');
});
